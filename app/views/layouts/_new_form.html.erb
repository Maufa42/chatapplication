<%= form_with(model: [@single_room, @message  ],remote: true) do |f|%>
  <%= f.text_field :body, id: 'chat-text', autocomplete: 'off'%>
  <%= f.submit data: {disable_with: false} class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 border border-blue-700 rounded"%>